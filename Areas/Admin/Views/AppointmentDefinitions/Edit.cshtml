@model WebApp.Models.AppointmentDefinition

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>AppointmentDefinitions</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AppointmentDefinitionID" />
            <div class="form-group">
                <label asp-for="PolyclinicID" class="controller-label"></label>
                <select asp-for="PolyclinicID" class="form-controller" asp-items="ViewBag.PolyclinicID"></select>
                <span asp-validation-for="PolyclinicID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Date" class="controller-label"></label>
                <select asp-for="Date" class="form-controller" asp-items="ViewBag.Date"></select>
                <span asp-validation-for="Date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TimeID" class="controller-label"></label>
                <select asp-for="TimeID" class="form-controller" asp-items="ViewBag.TimeID"></select>
                <span asp-validation-for="TimeID" class="text-danger"></span>
            </div>
           @*  <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="AppointmentStatus" /> @Html.DisplayNameFor(model => model.AppointmentStatus)
                </label>
            </div> *@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
