@model IEnumerable<WebApp.Models.AppointmentDefinition>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <theName>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Polyclinic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th></th>
        </tr>
    </theName>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Polyclinic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AppointmentDefinitionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AppointmentDefinitionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AppointmentDefinitionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
